"""Tool handlers. Each tool is a pure-ish function that takes validated
arguments, a resolved Config, and injected clients, and returns the standard
result envelope. Tools never raise to the transport.
"""
